import { Container, Title } from "./styles";

const Fallback = () => {
    return (
        <Container>
            <Title>You are offline</Title>
        </Container>
    );
};

export default Fallback;